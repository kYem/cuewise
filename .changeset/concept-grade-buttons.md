---
'@cuewise/browser-extension': patch
---

Polish the concept recall card. Grade buttons (Again/Good/Easy) become compact inline pills — label + interval on one line, soft tinted borders, sitting in a single row with the prompt — with Anki-style 1/2/3 keyboard shortcuts to grade without the mouse. A newly-added due card now joins the recall queue on the current new tab without needing a refresh, and the queue count stays in sync.
