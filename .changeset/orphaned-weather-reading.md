---
'@cuewise/browser-extension': patch
'@cuewise/macos': patch
'@cuewise/app': patch
---

A weather reading whose saved city can no longer be read is now cleared with it, instead of lingering behind a chip that renders nothing.
