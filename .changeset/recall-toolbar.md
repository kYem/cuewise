---
'@cuewise/browser-extension': patch
---

Add the recall card's bottom-dock toolbar: a ghosted control row with prev/next to browse the due queue, a reveal-then-next ring as the main action, a favorite toggle (new `isFavorite` on concepts, persisted), a due-count badge, and add — replacing the old text-link meta row.
