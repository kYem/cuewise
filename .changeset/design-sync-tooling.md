---
---

Tooling only: add `@cuewise/ui` design-system sync inputs under `.design-sync/` (config, authored previews, conventions, brand guidelines) so the component library publishes to claude.ai/design. No shipped package or extension-bundle change, so no version bump.
