---
'@cuewise/browser-extension': patch
---

fix(reminders): wrap long habit-pill text instead of overflowing — the label now wraps within the pill (which grows to fit) while the cadence/paused tag stays fixed, so long reminder names are no longer clipped.
