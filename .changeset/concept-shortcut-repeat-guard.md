---
'@cuewise/browser-extension': patch
'@cuewise/macos': patch
'@cuewise/app': patch
---

Holding **C** now swaps the quote for a concept card once instead of flickering between the two while the key is down. The same key is also ignored while the welcome screen is open, so a first-run tip no longer swaps something you can't see behind it.
