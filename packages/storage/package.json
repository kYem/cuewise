{
  "name": "@cuewise/storage",
  "version": "1.8.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@cuewise/shared": "workspace:*"
  },
  "devDependencies": {
    "@cuewise/test-utils": "workspace:*",
    "@types/chrome": "catalog:",
    "jsdom": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
