/**
 * How a component paints itself: 'overlay' is white-on-dark for a photo background,
 * 'surface' uses theme tokens for a page whose background follows the colour theme.
 */
export type ChromeVariant = 'overlay' | 'surface';
