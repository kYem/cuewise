{
  "name": "@cuewise/sync-engine",
  "version": "0.1.3",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@cuewise/shared": "workspace:*",
    "@cuewise/storage": "workspace:*",
    "@cuewise/crypto": "workspace:*",
    "@cuewise/sync-client": "workspace:*"
  },
  "devDependencies": {
    "@cuewise/test-utils": "workspace:*",
    "@types/chrome": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
